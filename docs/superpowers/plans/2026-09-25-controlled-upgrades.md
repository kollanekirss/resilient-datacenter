# Controlled application upgrade implementation

Follow the controlled-upgrades design and ongoing goal mandate. Work in this isolated checkout, inline. Never start downloaded server/client/container code on the developer computer. Unit tests use file fixtures and fake runtime interfaces; actual acceptance runs only on disposable GitHub-hosted Ubuntu.

1. Add a strict current/predecessor image catalogue. Write failing tests for exact supported source/target sets and rejection of mixed, arbitrary or downgraded sets. Teach backup ownership and data validation to recognize the exact predecessors without relaxing new-install identity. Verify frozen backup dependency closure.
2. Define the upgrade journal and fixed paths. Write transaction tests before implementation: ownership/component binding, exclusive pending operations, captured backup identity/age, failure rollback, interrupted recovery and irreversible commit. Reuse the durable filesystem and owned isolation helpers with retained untouched original directories; do not use ordinary restore rollback on an upgraded database. Persist rollback commit before ingress release.
3. Implement bounded read-only upgrade preflight, target artifact pull/verification and stopped-service backup/upload/download. Serialize through the existing operation locks and reject unrelated pending changes. Add an internal expected-transaction capture interface only if required; never a CLI override to bypass a pending restore.
4. Implement runtime staging and fixed Synapse/Nextcloud migrations. Keep stable service/network identity and secrets, preserve original code/runtime bytes, block external writes until commit, and keep partner connectors suspended through recovery. Update backup scope/frozen runtime only under the transaction contract.
5. Add check/apply/recover commands, guided navigation, clear maintenance and post-upgrade instructions, and status reporting for a pending upgrade. Preserve existing command behavior and privacy constraints.
6. Add guarded predecessor installation fixtures and actual current-version upgrade tests. Create real application data first, verify actual version/data after promotion, inject failure after migration and verify original code+data recovery. Exercise interruption and post-commit completion separately. Require a new-version backup/restore.
7. Run focused tests, complete local/syntax checks and native workflows. Inspect failures, review the full diff, update evidence, and integrate only passing supported paths. Keep all external acceptance requirements explicit and continue the remaining product goals.

Execution record: both native package paths passed run 36163148509 at a9ffb77. Inline author review checked precommit rollback versus irreversible commitment, retained original directories, stopped-container ownership, startup isolation and frozen-runtime dependencies. The reviewed infrastructure main branch is merged into this checkout; integration checks and merge follow.
