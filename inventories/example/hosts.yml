# Deliberately invalid placeholders. Copy into inventories/lab/hosts.yml and edit.
all:
  vars:
    headscale_hostname: control.example.com
    derp_hostname: relay.example.com
    enrollment_admin: lab-admin
    derper_artifact: /absolute/path/to/server-connectivity/artifacts/derper-linux-amd64
    derper_sha256: REPLACE_WITH_BUILD_OUTPUT
    test_ca_certificate: /absolute/path/to/certificates/test-ca.crt
  children:
    controller:
      hosts:
        control-01:
          ansible_host: 192.0.2.10
          ansible_user: ubuntu
          tls_certificate: /absolute/path/to/certificates/control-fullchain.crt
          tls_private_key: /absolute/path/to/certificates/control.key
    relay:
      hosts:
        relay-01:
          ansible_host: 192.0.2.20
          ansible_user: ubuntu
          tls_certificate: /absolute/path/to/certificates/relay-fullchain.crt
          tls_private_key: /absolute/path/to/certificates/relay.key
    peers:
      hosts:
        server-a:
          ansible_host: 192.0.2.30
          ansible_user: ubuntu
          node_tag: tag:institution-a-server
          peer_name: server-b
          test_dns_name: a.pilot.example.com
          tls_certificate: /absolute/path/to/certificates/a-fullchain.crt
          tls_private_key: /absolute/path/to/certificates/a.key
        server-b:
          ansible_host: 192.0.2.40
          ansible_user: ubuntu
          node_tag: tag:institution-b-server
          peer_name: server-a
          test_dns_name: b.pilot.example.com
          tls_certificate: /absolute/path/to/certificates/b-fullchain.crt
          tls_private_key: /absolute/path/to/certificates/b.key
